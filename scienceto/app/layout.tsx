import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "./components/Navbar";
import Footer from "./components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Jheel Patel | Cloud Systems & Software Engineer",
  description: "Code. Cloud. Complexity. AI. Documenting my journey through the modern tech stack and distributed systems engineering.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        {/* Add padding-top so content doesn't hide behind the sticky nav */}
        <main className="pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
